# Analysis scripts

This folder contains scripts/notebooks for analyzing benchmark runs and traces without mixing them into the benchmark output directories.

- `analysis/benchmarks/`: tools for comparing benchmark reports and inspecting traces/events.
- `analysis/hetzner/`: Hetzner-specific investigation notebooks (OOM / stall analysis).

